== 📥 Installing the APK (Android) ==

1. Download the file `CineExplorer-v1.0.apk`.
2. Open the file on your Android smartphone.
3. If prompted, "Allow installation from unknown sources" (this is normal for apps not installed via Play Store).
4. Tap "Install" and enjoy!

=== 💻 Building from Source (Developers) ===

REQUIREMENTS:
- Node.js (v18+)
- Java JDK 17
- Android Studio (for SDK and Emulator)

STEPS:
1. Clone the repository:
   git clone https://github.com/graz68a/CineExplorer.git

2. Enter the folder:
   cd CineExplorer

3. Install dependencies:
   npm install

4. Configure API Keys:
   - Create a file named `.env` in the root folder.
   - Add your TMDB Keys (see .env.example).

5. Build the Android App:
   npm run build
   npx cap sync android
   
   # For Windows users to build APK:
   cd android
   ./gradlew assembleDebug

   The APK will be located at: 
   android/app/build/outputs/apk/debug/app-debug.apk
